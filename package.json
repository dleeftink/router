{
  "name": "router",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "needle": "3.2.0",
    "axios": "1.3.4"
  },
  "scripts": {
    "build": "npm run pre-build && next build && next export"
  }
}
